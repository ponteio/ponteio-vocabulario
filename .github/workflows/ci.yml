name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    name: vocabulário — Turtle e FF-04
    uses: gicc-ufrn/.github/.github/workflows/mise-ci.yml@main
    with:
      tarefas: ci
